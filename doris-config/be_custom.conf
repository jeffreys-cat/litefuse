# Doris BE Custom Configuration (loaded after be.conf on startup)

# --- EO ledger load headroom (2026-07-28 incident trigger) ---
# blob_storage_file_log / traces_scalar are tiny high-frequency stream loads:
# versions concentrate on few tablets and hit the default 2000 cap (E-235)
# whenever compaction lags, failing group jobs mid-pipeline.
max_tablet_version_num = 5000
